partX_output.png為每個part輸出結果的截圖

partX_output.png is a screenshot of the output result for each part.

其他的png檔為py檔所產生的圖表

The remaining PNG files are charts generated by the Python files.

json檔為改寫前後的文本資料

The JSON files contain the text data before and after the rewrite.
